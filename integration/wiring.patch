diff --git a/docs/persistence-catalog.md b/docs/persistence-catalog.md
index c400298f7d..608a8c9676 100644
--- a/docs/persistence-catalog.md
+++ b/docs/persistence-catalog.md
@@ -405,6 +405,23 @@ Source: [`packages/compaction/compaction/src/types.ts:33`](../packages/compactio
 
 Source: [`packages/feedback/command-feedback/src/index.ts:62`](../packages/feedback/command-feedback/src/index.ts)
 
+### `file/*`
+
+<a id="fileselection--log-only"></a>
+
+#### `file/selection` — log-only
+
+```ts persistence-catalog
+/**
+ * The user changed the file tree's selected-file set. Replaces the prior
+ * selection (an empty `files` clears it). Logged so the model-visible
+ * "selected files" context is reconstructable from the session log.
+ */
+'file/selection': { files: string[] }
+```
+
+Source: [`packages/context/file-selection/src/types.ts:13`](../packages/context/file-selection/src/types.ts)
+
 ### `goal/*`
 
 <a id="goalchange--log-only"></a>
diff --git a/packages/api/remotes/package.json b/packages/api/remotes/package.json
index 42a7d14070..ea07171d13 100644
--- a/packages/api/remotes/package.json
+++ b/packages/api/remotes/package.json
@@ -65,6 +65,7 @@
     "@deepseek-ai/dsh-credentials": "workspace:^",
     "@deepseek-ai/dsh-goal": "workspace:^",
     "@deepseek-ai/dsh-cordis-host-runner": "workspace:^",
+    "@deepseek-ai/dsh-host-file-tree": "workspace:^",
     "@deepseek-ai/dsh-host-plugin-inventory": "workspace:^",
     "@deepseek-ai/dsh-invariants": "workspace:^",
     "@deepseek-ai/dsh-agent-presets": "workspace:^",
@@ -82,6 +83,7 @@
     "@deepseek-ai/dsh-credentials": "workspace:^",
     "@deepseek-ai/dsh-goal": "workspace:^",
     "@deepseek-ai/dsh-cordis-host-runner": "workspace:^",
+    "@deepseek-ai/dsh-host-file-tree": "workspace:^",
     "@deepseek-ai/dsh-host-plugin-inventory": "workspace:^",
     "@deepseek-ai/dsh-invariants": "workspace:^",
     "@deepseek-ai/dsh-agent-presets": "workspace:^",
diff --git a/packages/api/remotes/src/client/index.ts b/packages/api/remotes/src/client/index.ts
index 210ec0bf80..7bd7fe287f 100644
--- a/packages/api/remotes/src/client/index.ts
+++ b/packages/api/remotes/src/client/index.ts
@@ -26,6 +26,7 @@ export type {} from '@deepseek-ai/dsh-credentials/types'
 export type {} from '@deepseek-ai/dsh-llm/types'
 export type {} from '@deepseek-ai/dsh-agent-presets/types'
 export type {} from '@deepseek-ai/dsh-settings/types'
+export type {} from '@deepseek-ai/dsh-host-file-tree/types'
 
 /**
  * The carrier's Client-facing types, re-exported so a business package names one
@@ -34,7 +35,8 @@ export type {} from '@deepseek-ai/dsh-settings/types'
  */
 export type {
   ClientResponse, ConfigurableProviderView, ConnectionHandle, ConnectionSinks, ContentBlock,
-  CredentialView, DirectoryListing, DiscoveredModelView, HistoryEntry, HostFrame, IApiClient,
+  CredentialView, DirectoryListing, DiscoveredModelView, FileTreeEntry, FileTreeEntryKind, FileTreeGitStatus,
+  FileTreeListing, FileTreeSearchResult, HistoryEntry, HostFrame, IApiClient,
   MessageId, ModelCatalogFailure, ModelProviderGroup, ModelReasoningEffort, ModelSelection,
   MuxFrame, PromptContentPart, QuestionResponsePayload, QueueAction, RpcError, RpcId, RpcReceipt,
   RpcRequest, RpcResponse, RpcResult, SessionId, SessionModels, SessionSearchItem,
diff --git a/packages/api/remotes/src/index.ts b/packages/api/remotes/src/index.ts
index 6572c11938..13156cf39e 100644
--- a/packages/api/remotes/src/index.ts
+++ b/packages/api/remotes/src/index.ts
@@ -13,6 +13,7 @@ import type {} from '@deepseek-ai/dsh-credentials/types'
 import type {} from '@deepseek-ai/dsh-llm/types'
 import type {} from '@deepseek-ai/dsh-agent-presets/types'
 import type {} from '@deepseek-ai/dsh-settings/types'
+import type {} from '@deepseek-ai/dsh-host-file-tree/types'
 
 export {
   ApiRemoteSessionNotFound,
diff --git a/packages/api/remotes/src/remote-events.ts b/packages/api/remotes/src/remote-events.ts
index 2174336532..da2b243689 100644
--- a/packages/api/remotes/src/remote-events.ts
+++ b/packages/api/remotes/src/remote-events.ts
@@ -18,6 +18,7 @@ export const API_REMOTE_FORWARDED_EVENTS = [
   'agent-preset/selected',
   'commands/change',
   'credentials/updated',
+  'filetree/change',
   'cordis/request-run',
   'cordis/request-run-resolved',
   'cordis/dynamic-package',
diff --git a/packages/api/remotes/tsconfig.host.json b/packages/api/remotes/tsconfig.host.json
index 61eae810f4..d795625858 100644
--- a/packages/api/remotes/tsconfig.host.json
+++ b/packages/api/remotes/tsconfig.host.json
@@ -22,6 +22,9 @@
     {
       "path": "../../core/session"
     },
+    {
+      "path": "../../host/file-tree"
+    },
     {
       "path": "../../credentials/credentials"
     },
diff --git a/packages/bundle/web-app/cordis.patch.yml b/packages/bundle/web-app/cordis.patch.yml
index b06e63b54f..08da4119dd 100644
--- a/packages/bundle/web-app/cordis.patch.yml
+++ b/packages/bundle/web-app/cordis.patch.yml
@@ -90,6 +90,16 @@
     - id: directory-picker
       name: '@deepseek-ai/dsh-host-directory-picker-auto'
 
+    # Workspace file-tree backend: level listing with git status plus Chokidar
+    # change events the api-gateway forwards to clients as `filetree/change`.
+    - id: file-tree
+      name: '@deepseek-ai/dsh-host-file-tree-local'
+
+    # Logs the file tree's selected-file set and renders it into the system
+    # prompt, so the model knows which files the user marked.
+    - id: file-selection
+      name: '@deepseek-ai/dsh-file-selection'
+
     # Read-only projection of current Loader entries for trusted client RPCs.
     - id: plugin-inventory
       name: '@deepseek-ai/dsh-host-plugin-inventory'
@@ -219,6 +229,10 @@
     - id: ui-workspace
       name: '@deepseek-ai/dsh-client-ui-workspace'
 
+    # Sidebar file tree: fills the shell's sidebar.filetree hole.
+    - id: ui-file-tree
+      name: '@deepseek-ai/dsh-client-ui-file-tree'
+
     # Input triggers: the '/' | '@' pipeline (ui-input-trigger), the command surface over
     # it (ui-commands), and the two reference sources (ui-skill / ui-subagent).
     - id: ui-input-trigger
diff --git a/packages/bundle/web-app/package.json b/packages/bundle/web-app/package.json
index ca4709a2a9..830a10beaf 100644
--- a/packages/bundle/web-app/package.json
+++ b/packages/bundle/web-app/package.json
@@ -59,6 +59,7 @@
     "@deepseek-ai/dsh-client-ui-deliverables": "workspace:^",
     "@deepseek-ai/dsh-client-ui-directory-picker-browse": "workspace:^",
     "@deepseek-ai/dsh-client-ui-directory-picker-native": "workspace:^",
+    "@deepseek-ai/dsh-client-ui-file-tree": "workspace:^",
     "@deepseek-ai/dsh-client-ui-message-feedback": "workspace:^",
     "@deepseek-ai/dsh-client-ui-goal": "workspace:^",
     "@deepseek-ai/dsh-client-ui-layout": "workspace:^",
@@ -85,12 +86,14 @@
     "@deepseek-ai/dsh-code-runtime-worker-thread": "workspace:^",
     "@deepseek-ai/dsh-cordis-client-runner": "workspace:^",
     "@deepseek-ai/dsh-cordis-host-runner": "workspace:^",
+    "@deepseek-ai/dsh-file-selection": "workspace:^",
     "@deepseek-ai/dsh-web-frontend": "workspace:^",
     "@deepseek-ai/dsh-host-frontend-static": "workspace:^",
     "@deepseek-ai/dsh-host-apiproxy": "workspace:^",
     "@deepseek-ai/dsh-host-directory-picker-auto": "workspace:^",
     "@deepseek-ai/dsh-host-directory-picker-browse": "workspace:^",
     "@deepseek-ai/dsh-host-directory-picker-native": "workspace:^",
+    "@deepseek-ai/dsh-host-file-tree-local": "workspace:^",
     "@deepseek-ai/dsh-host-plugin-inventory": "workspace:^",
     "@deepseek-ai/dsh-host-webserver": "workspace:^",
     "@deepseek-ai/dsh-message-feedback": "workspace:^",
diff --git a/packages/client/connection/src/client/api.ts b/packages/client/connection/src/client/api.ts
index 1b7627b293..7414cf2061 100644
--- a/packages/client/connection/src/client/api.ts
+++ b/packages/client/connection/src/client/api.ts
@@ -8,7 +8,7 @@
 export type {
   ApiProxy, SessionsApi, SessionSearchItem, SessionSummary, PromptContentPart, HostApi, EventsApi, MuxFrame, HostFrame,
   ApprovalResponsePayload, QuestionResponsePayload, HistoryEntry, ToolEventView,
-  DirectoryEntry, DirectoryListing,
+  DirectoryEntry, DirectoryListing, FileTreeEntry, FileTreeEntryKind, FileTreeGitStatus, FileTreeListing, FileTreeSearchResult,
   ResponseValue, WorkspaceApi, WorkspaceId, WorkspaceView,
   SkillsApi, SkillEntry,
   ModelCatalogFailure, ModelCatalogModel, ModelProviderGroup, ModelReasoning,
diff --git a/packages/client/connection/src/client/fixture.ts b/packages/client/connection/src/client/fixture.ts
index dd0566486e..7c8425e468 100644
--- a/packages/client/connection/src/client/fixture.ts
+++ b/packages/client/connection/src/client/fixture.ts
@@ -2563,6 +2563,11 @@ function createFixtureWorld(options: FixtureOptions): FixtureWorld {
       },
       openPath: request => ok(request, { opened: true as const }),
     },
+    filetree: {
+      list: request => ok(request, { path: request.payload.path, entries: [], truncated: false }),
+      search: request => ok(request, { path: request.payload.path, matches: [], truncated: false }),
+      select: request => ok(request, { selected: [...request.payload.files] }),
+    },
     workspace: {
       list: request => ok(request, {
         items: workspaces.map(w => ({ ...w })),
@@ -3098,6 +3103,9 @@ export class FixtureApiClient extends AbstractApiClient {
       case 'host.listDirectory': return this.api.host.listDirectory(request, new AbortController().signal)
       case 'host.createDirectory': return this.api.host.createDirectory(request)
       case 'host.openPath': return this.api.host.openPath(request, new AbortController().signal)
+      case 'filetree.list': return this.api.filetree.list(request, signal)
+      case 'filetree.search': return this.api.filetree.search(request, signal)
+      case 'filetree.select': return this.api.filetree.select(request)
       case 'workspace.list': return this.api.workspace.list(request)
       case 'workspace.create': return this.api.workspace.create(request)
       case 'workspace.rename': return this.api.workspace.rename(request)
diff --git a/packages/client/connection/src/client/index.ts b/packages/client/connection/src/client/index.ts
index c4470b13aa..d311669769 100644
--- a/packages/client/connection/src/client/index.ts
+++ b/packages/client/connection/src/client/index.ts
@@ -16,7 +16,7 @@ import type { ClientConnectionRpc } from '../rpc.ts'
 export type {
   ApiProxy, SessionsApi, SessionSearchItem, SessionSummary, PromptContentPart, HostApi, EventsApi, MuxFrame, HostFrame,
   ApprovalResponsePayload, QuestionResponsePayload, HistoryEntry, ToolEventView,
-  DirectoryEntry, DirectoryListing,
+  DirectoryEntry, DirectoryListing, FileTreeEntry, FileTreeEntryKind, FileTreeGitStatus, FileTreeListing, FileTreeSearchResult,
   ToolCallView, ToolResultView, WorkspaceApi, WorkspaceId, WorkspaceView,
   SkillsApi, SkillEntry,
   ModelCatalogFailure, ModelCatalogModel, ModelProviderGroup, ModelReasoning,
diff --git a/packages/client/connection/tests/fake-api.client.ts b/packages/client/connection/tests/fake-api.client.ts
index bee4fc0ce0..205d6968c1 100644
--- a/packages/client/connection/tests/fake-api.client.ts
+++ b/packages/client/connection/tests/fake-api.client.ts
@@ -148,6 +148,12 @@ export class FakeApiClient implements IApiClient {
     openPath: payload => this.record('host.openPath', payload, this.onOpenPath(payload)),
   }
 
+  readonly filetree: IApiClient['filetree'] = {
+    list: payload => this.record('filetree.list', payload, Promise.resolve(ok({ path: '/f', entries: [], truncated: false }))),
+    search: payload => this.record('filetree.search', payload, Promise.resolve(ok({ path: '/f', matches: [], truncated: false }))),
+    select: payload => this.record('filetree.select', payload, Promise.resolve(ok({ selected: [...(payload as { files: string[] }).files] }))),
+  }
+
   readonly workspace: IApiClient['workspace'] = {
     list: (payload: unknown) => this.record('workspace.list', payload, Promise.resolve(ok({ items: [], archivedSessionIds: [] }))),
     create: (payload: unknown) => this.record('workspace.create', payload, Promise.resolve(ok({
diff --git a/packages/client/runtime/src/client/contract/workspaces.ts b/packages/client/runtime/src/client/contract/workspaces.ts
index 4012086c3d..f85644f8f0 100644
--- a/packages/client/runtime/src/client/contract/workspaces.ts
+++ b/packages/client/runtime/src/client/contract/workspaces.ts
@@ -6,7 +6,7 @@
  * the concrete class. Widening this interface is the explicit act of
  * widening what features may do to the workspaces domain.
  */
-import type { DirectoryListing, SessionId, WorkspaceId, WorkspaceView } from '@deepseek-ai/dsh-api-remotes/client'
+import type { DirectoryListing, FileTreeListing, FileTreeSearchResult, SessionId, WorkspaceId, WorkspaceView } from '@deepseek-ai/dsh-api-remotes/client'
 import type { WorkspaceListState } from '../workspaces/service.ts'
 import type { ObservableSnapshot } from './store.ts'
 
@@ -58,6 +58,27 @@ export interface IWorkspaces {
    * @param path - absolute or host-resolvable path.
    */
   openPath(path: string): Promise<void>
+  /**
+   * List one directory level through the file-tree capability.
+   * @param path - absolute directory to list (a session's workspace root).
+   * @param signal - aborts the wire request and the Host's scan.
+   * @returns the level's children with per-file git status.
+   */
+  listDir(path: string, signal?: AbortSignal): Promise<FileTreeListing>
+  /**
+   * Search file and directory names under a session's workspace root.
+   * @param root - absolute search root (a session's workspace root).
+   * @param query - non-blank, case-insensitive name substring.
+   * @param signal - aborts the wire request and the Host's scan.
+   * @returns flat name matches (ancestors not included), capped and flagged.
+   */
+  searchEntries(root: string, query: string, signal?: AbortSignal): Promise<FileTreeSearchResult>
+  /**
+   * Record a session's selected-file set (the file tree's multi-select).
+   * @param sessionId - the session whose selection changed.
+   * @param files - the complete selected-path set.
+   */
+  selectFiles(sessionId: SessionId, files: string[]): Promise<void>
   /**
    * Rename a Workspace.
    * @param workspaceId - target workspace.
diff --git a/packages/client/runtime/src/client/index.ts b/packages/client/runtime/src/client/index.ts
index b5433bb573..8329f3af8f 100644
--- a/packages/client/runtime/src/client/index.ts
+++ b/packages/client/runtime/src/client/index.ts
@@ -64,7 +64,8 @@ export type { SubagentAddress, JobView } from '@deepseek-ai/dsh-client-connectio
 export type { WorkspaceListPhase } from './workspaces/manager.ts'
 export type { WorkspaceListState } from './workspaces/service.ts'
 export type {
-  DirectoryEntry, DirectoryListing, WorkspaceId, WorkspaceView,
+  DirectoryEntry, DirectoryListing, FileTreeEntry, FileTreeEntryKind, FileTreeGitStatus, FileTreeListing,
+  FileTreeSearchResult, WorkspaceId, WorkspaceView,
 } from '@deepseek-ai/dsh-client-connection/client'
 // Runtime owns the snapshot store; web-react only binds it to React.
 export { createSnapshotStore, defineStore, shallowEqual } from './contract/store.ts'
diff --git a/packages/client/runtime/src/client/workspaces/service.ts b/packages/client/runtime/src/client/workspaces/service.ts
index c23e3b3a6c..a1bba436e2 100644
--- a/packages/client/runtime/src/client/workspaces/service.ts
+++ b/packages/client/runtime/src/client/workspaces/service.ts
@@ -2,7 +2,7 @@
 
 import type { Context } from '@deepseek-ai/cordis'
 import type {
-  DirectoryListing, IApiClient, RpcError,
+  DirectoryListing, FileTreeListing, FileTreeSearchResult, IApiClient, RpcError,
   SessionId, WorkspaceId, WorkspaceView,
 } from '@deepseek-ai/dsh-api-remotes/client'
 import type { SnapshotStore } from '../contract/store.ts'
@@ -249,6 +249,41 @@ export class WorkspaceRuntime implements IWorkspaces {
     }
   }
 
+  /**
+   * List one directory level through the file-tree capability.
+   * @param path - absolute directory to list (a session's workspace root).
+   * @param signal - aborts the wire request and the Host's scan.
+   * @returns the level's children with per-file git status.
+   */
+  async listDir(path: string, signal?: AbortSignal): Promise<FileTreeListing> {
+    const response = await this.api.filetree.list({ path }, signal)
+    if (!response.result.ok) throw new Error(`file tree list failed: ${response.result.error.message}`)
+    return response.result.value
+  }
+
+  /**
+   * Search file and directory names under a session's workspace root.
+   * @param root - absolute search root (a session's workspace root).
+   * @param query - non-blank, case-insensitive name substring.
+   * @param signal - aborts the wire request and the Host's scan.
+   * @returns flat name matches (ancestors not included), capped and flagged.
+   */
+  async searchEntries(root: string, query: string, signal?: AbortSignal): Promise<FileTreeSearchResult> {
+    const response = await this.api.filetree.search({ path: root, query }, signal)
+    if (!response.result.ok) throw new Error(`file tree search failed: ${response.result.error.message}`)
+    return response.result.value
+  }
+
+  /**
+   * Record a session's selected-file set (the file tree's multi-select).
+   * @param sessionId - the session whose selection changed.
+   * @param files - the complete selected-path set.
+   */
+  async selectFiles(sessionId: SessionId, files: string[]): Promise<void> {
+    const response = await this.api.filetree.select({ sessionId, files })
+    if (!response.result.ok) throw new Error(`file tree selection failed: ${response.result.error.message}`)
+  }
+
   /**
    * Rename a Workspace.
    * @param workspaceId - target workspace.
diff --git a/packages/client/runtime/tests/fake-api.client.ts b/packages/client/runtime/tests/fake-api.client.ts
index 33a0efbbfd..9936616ba9 100644
--- a/packages/client/runtime/tests/fake-api.client.ts
+++ b/packages/client/runtime/tests/fake-api.client.ts
@@ -130,6 +130,11 @@ export class FakeApiClient implements IApiClient {
   onCreateDirectory: (payload: unknown) => Promise<RpcResponse<{ path: string }>> =
     () => Promise.resolve(ok({ path: '/home/fake/new' }))
 
+  onListDir: (payload: unknown) => Promise<RpcResponse<{ path: string; entries: never[]; truncated: boolean }>> =
+    () => Promise.resolve(ok({ path: '/f', entries: [], truncated: false }))
+  onSelect: (payload: unknown) => Promise<RpcResponse<{ selected: string[] }>> =
+    () => Promise.resolve(ok({ selected: [] }))
+
   private readonly muxConns: StreamConn<MuxFrame>[] = []
   private readonly hostConns: StreamConn<HostFrame>[] = []
   lastSearchSignal: AbortSignal | undefined
@@ -182,6 +187,12 @@ export class FakeApiClient implements IApiClient {
     openPath: (payload: unknown) => this.record('host.openPath', payload, this.onOpenPath(payload)),
   }
 
+  readonly filetree: IApiClient['filetree'] = {
+    list: (payload: unknown) => this.record('filetree.list', payload, this.onListDir(payload)),
+    search: (payload: unknown) => this.record('filetree.search', payload, Promise.resolve(ok({ path: '/f', matches: [], truncated: false }))),
+    select: (payload: unknown) => this.record('filetree.select', payload, this.onSelect(payload)),
+  }
+
   // The archive-set field defaults at the binding below so list stubs keep
   // the pre-archive `{ items }` shape; a stub carrying the field wins.
   onWorkspaceList: (payload: unknown) => Promise<RpcResponse<{ items: never[]; archivedSessionIds?: never[] }>> =
diff --git a/packages/client/ui-sidebar/src/client/SidebarRoot.module.css b/packages/client/ui-sidebar/src/client/SidebarRoot.module.css
index 300075b88d..19541ec364 100644
--- a/packages/client/ui-sidebar/src/client/SidebarRoot.module.css
+++ b/packages/client/ui-sidebar/src/client/SidebarRoot.module.css
@@ -241,6 +241,36 @@
   padding-left: 0;
 }
 
+/* Workspace/Files view toggle: a two-segment control above the region. */
+.viewToggle {
+  flex: none;
+  display: flex;
+  gap: 2px;
+  margin: 0 2px 8px;
+  padding: 2px;
+  border: 1px solid var(--dsw-alias-border-l2);
+  border-radius: 10px;
+}
+
+.viewSegment {
+  flex: 1;
+  height: 26px;
+  padding: 0 8px;
+  border: none;
+  border-radius: 7px;
+  background: transparent;
+  color: var(--dsw-alias-label-secondary);
+  font-size: 13px;
+  font-weight: 500;
+  line-height: 1;
+  cursor: pointer;
+}
+
+.viewSegmentActive {
+  background: var(--dsw-alias-interactive-bg-hover);
+  color: var(--dsw-alias-label-primary);
+}
+
 /* Footer seats: additive actions stack above Settings. Each occupant owns its
    button geometry and hover chrome. */
 .footArea {
diff --git a/packages/client/ui-sidebar/src/client/SidebarRoot.tsx b/packages/client/ui-sidebar/src/client/SidebarRoot.tsx
index d3fe6a63c1..b56aea4fbc 100644
--- a/packages/client/ui-sidebar/src/client/SidebarRoot.tsx
+++ b/packages/client/ui-sidebar/src/client/SidebarRoot.tsx
@@ -59,6 +59,9 @@ export function SidebarRoot({
   }, [collapsed])
   const wide = !collapsed || !settled
 
+  // Workspace/Files region switch: shell-owned so both occupants share one control.
+  const [view, setView] = useState<'workspace' | 'files'>('workspace')
+
   // Freeze the content at its expanded width while it fades out (collapsed
   // && wide): the sliding column then clips it instead of reflowing it. The
   // rail layout (.collapsed styles) only applies once the fade settles.
@@ -169,13 +172,39 @@ export function SidebarRoot({
         </button>
       </Tooltip>
 
+      {wide && (
+        <div className={css.viewToggle}>
+          <button
+            type="button"
+            className={clsx(css.viewSegment, view === 'workspace' && css.viewSegmentActive)}
+            aria-pressed={view === 'workspace'}
+            onClick={() => { setView('workspace') }}
+          >
+            {t('view.workspace')}
+          </button>
+          <button
+            type="button"
+            className={clsx(css.viewSegment, view === 'files' && css.viewSegmentActive)}
+            aria-pressed={view === 'files'}
+            onClick={() => { setView('files') }}
+          >
+            {t('view.files')}
+          </button>
+        </div>
+      )}
+
       {/* The browsing region fills the column between the controls and the
           foot in both states; its rail icon column rides the same slot. */}
       <div className={css.regionArea}>
-        {renderSlot('sidebar.workspaces', {
-          wide,
-          expandSidebar: () => { if (collapsed) toggleSidebar() },
-        })}
+        {view === 'files'
+          ? renderSlot('sidebar.filetree', {
+            wide,
+            expandSidebar: () => { if (collapsed) toggleSidebar() },
+          })
+          : renderSlot('sidebar.workspaces', {
+            wide,
+            expandSidebar: () => { if (collapsed) toggleSidebar() },
+          })}
       </div>
 
       {/* Footer actions stack above Settings in both sidebar widths. */}
diff --git a/packages/client/ui-sidebar/src/client/contract/slots.ts b/packages/client/ui-sidebar/src/client/contract/slots.ts
index a8c4f2894d..89b6885c0b 100644
--- a/packages/client/ui-sidebar/src/client/contract/slots.ts
+++ b/packages/client/ui-sidebar/src/client/contract/slots.ts
@@ -22,6 +22,12 @@ declare module '@deepseek-ai/dsh-client-ui-slots' {
      * registers the browser.
      */
     'sidebar.workspaces': { kind: 'single'; scope: 'root'; owner: SidebarSectionOwnerProps }
+    /**
+     * The file-tree browsing region, toggled against the workspace browser by
+     * the shell's view control. Declared by this package's 'sidebar' entry;
+     * ui-file-tree registers the tree.
+     */
+    'sidebar.filetree': { kind: 'single'; scope: 'root'; owner: SidebarSectionOwnerProps }
     /**
      * The settings seat at the sidebar foot. Declared by this package's
      * 'sidebar' entry; ui-settings registers its trigger row + modal panel.
@@ -85,5 +91,5 @@ export type SidebarRootInjected = {
  */
 export type SidebarRootComponentProps =
   PropsRuntime<'sidebar'>
-  & PropsRenderSlots<'sidebar.workspaces' | 'sidebar.settings' | 'sidebar.footer.action'>
+  & PropsRenderSlots<'sidebar.workspaces' | 'sidebar.filetree' | 'sidebar.settings' | 'sidebar.footer.action'>
   & SidebarRootInjected & PropsLocale<'sidebar'>
diff --git a/packages/client/ui-sidebar/src/client/index.ts b/packages/client/ui-sidebar/src/client/index.ts
index 0bd2c98295..ed4fa95d72 100644
--- a/packages/client/ui-sidebar/src/client/index.ts
+++ b/packages/client/ui-sidebar/src/client/index.ts
@@ -46,6 +46,7 @@ export function apply(ctx: ClientContext): void {
       // registers the foot trigger + settings panel.
       children: {
         'sidebar.workspaces': { kind: 'single', scope: 'root' },
+        'sidebar.filetree': { kind: 'single', scope: 'root' },
         'sidebar.settings': { kind: 'single', scope: 'root' },
         'sidebar.footer.action': { kind: 'list', scope: 'root' },
       },
diff --git a/packages/client/ui-sidebar/src/client/locales.ts b/packages/client/ui-sidebar/src/client/locales.ts
index 8cf5ac6d7b..2a8ee7ccf5 100644
--- a/packages/client/ui-sidebar/src/client/locales.ts
+++ b/packages/client/ui-sidebar/src/client/locales.ts
@@ -6,6 +6,8 @@ export const zh = {
   'session.new.label': '新建会话',
   'toggle.open': '打开侧边栏',
   'toggle.collapse': '收起侧边栏',
+  'view.workspace': '工作区',
+  'view.files': '文件',
 } satisfies Record<string, string>
 
 /** The sidebar namespace key union. */
@@ -17,4 +19,6 @@ export const en = {
   'session.new.label': 'New session',
   'toggle.open': 'Open sidebar',
   'toggle.collapse': 'Collapse sidebar',
+  'view.workspace': 'Workspace',
+  'view.files': 'Files',
 } satisfies Record<SidebarKey, string>
diff --git a/packages/core/session/src/known-event-types.ts b/packages/core/session/src/known-event-types.ts
index d65935f1b8..9cfe71c4b3 100644
--- a/packages/core/session/src/known-event-types.ts
+++ b/packages/core/session/src/known-event-types.ts
@@ -31,6 +31,7 @@ export const KNOWN_SESSION_EVENT_TYPES: ReadonlySet<string> = new Set([
   'compaction/start',
   'compaction/summary',
   'feedback/record',
+  'file/selection',
   'goal/change',
   'hook/invoked',
   'hook/result',
diff --git a/packages/host/apiproxy/package.json b/packages/host/apiproxy/package.json
index ba83efedb5..3691b244b6 100644
--- a/packages/host/apiproxy/package.json
+++ b/packages/host/apiproxy/package.json
@@ -54,6 +54,8 @@
     "@deepseek-ai/dsh-credentials": "workspace:^",
     "@deepseek-ai/dsh-goal": "workspace:^",
     "@deepseek-ai/dsh-host-directory-picker": "workspace:^",
+    "@deepseek-ai/dsh-host-file-tree": "workspace:^",
+    "@deepseek-ai/dsh-file-selection": "workspace:^",
     "@deepseek-ai/dsh-llm": "workspace:^",
     "@deepseek-ai/dsh-native-command": "workspace:^",
     "@deepseek-ai/dsh-session": "workspace:^",
diff --git a/packages/host/apiproxy/src/api-proxy.ts b/packages/host/apiproxy/src/api-proxy.ts
index bda99362b6..3a293890c1 100644
--- a/packages/host/apiproxy/src/api-proxy.ts
+++ b/packages/host/apiproxy/src/api-proxy.ts
@@ -99,6 +99,8 @@ import type {
 } from '@deepseek-ai/dsh-user-questions'
 import { UserQuestionError } from '@deepseek-ai/dsh-user-questions'
 import { DirectoryPickerError } from '@deepseek-ai/dsh-host-directory-picker'
+import { FileTreeError } from '@deepseek-ai/dsh-host-file-tree'
+import type {} from '@deepseek-ai/dsh-file-selection'
 import {
   ApiRemoteSessionNotFound as SessionNotFound,
   ApiRemoteSubagentSessionOwnership as SubagentSessionOwnership,
@@ -632,6 +634,14 @@ function directoryError(error: unknown): RpcError {
   return { code: 'internal', message: error instanceof Error ? error.message : String(error), details: {} }
 }
 
+/** Map a file-tree listing failure onto the wire error vocabulary (unknown throws stay internal). */
+function fileTreeError(error: unknown): RpcError {
+  if (error instanceof FileTreeError) {
+    return { code: 'tree-unreadable', message: error.message, details: { path: error.path } }
+  }
+  return { code: 'internal', message: error instanceof Error ? error.message : String(error), details: {} }
+}
+
 /** Resolved Agent model and project-directory defaults consumed by the API implementation. */
 export interface ApiProxyDefaults {
   /**
@@ -3010,6 +3020,63 @@ export function createApiProxy(ctx: Context, defaults: ApiProxyDefaults): ApiPro
       },
     },
 
+    filetree: {
+      async list(request, signal) {
+        const fileTree = ctx.get('fileTree')
+        if (fileTree === undefined) {
+          return err(request, {
+            code: 'file-tree-unavailable',
+            message: 'filetree.list needs a file-tree backend; none is composed',
+            details: {},
+          })
+        }
+        try {
+          return ok(request, await fileTree.listDir(request.payload.path, signal))
+        } catch (error: unknown) {
+          // An abort is the caller's own timeout/disconnect, not a server failure.
+          if (signal.aborted) {
+            return err(request, { code: 'cancelled', message: 'file tree listing was aborted', details: {} })
+          }
+          return err(request, fileTreeError(error))
+        }
+      },
+
+      async search(request, signal) {
+        const fileTree = ctx.get('fileTree')
+        if (fileTree === undefined) {
+          return err(request, {
+            code: 'file-tree-unavailable',
+            message: 'filetree.search needs a file-tree backend; none is composed',
+            details: {},
+          })
+        }
+        try {
+          return ok(request, await fileTree.search(request.payload.path, request.payload.query, signal))
+        } catch (error: unknown) {
+          // An abort is the caller's own timeout/disconnect, not a server failure.
+          if (signal.aborted) {
+            return err(request, { code: 'cancelled', message: 'file tree search was aborted', details: {} })
+          }
+          return err(request, fileTreeError(error))
+        }
+      },
+
+      async select(request) {
+        const fileSelection = ctx.get('fileSelection')
+        if (fileSelection === undefined) {
+          return err(request, {
+            code: 'file-tree-unavailable',
+            message: 'filetree.select needs a file-selection service; none is composed',
+            details: {},
+          })
+        }
+        const found = await agentFor(request.payload.sessionId)
+        if ('error' in found) return err(request, found.error)
+        fileSelection.select(found.agent.session, request.payload.files)
+        return ok(request, { selected: [...request.payload.files] })
+      },
+    },
+
     goals: {
       // Mutations only — the read side is the 'goal' session projection.
       // Every verb resolves the session's agent (agentFor: implicit cold
diff --git a/packages/host/apiproxy/src/api/index.ts b/packages/host/apiproxy/src/api/index.ts
index b5e1d1ffd9..a5cb5b78e1 100644
--- a/packages/host/apiproxy/src/api/index.ts
+++ b/packages/host/apiproxy/src/api/index.ts
@@ -6,6 +6,7 @@
 
 import type { SessionsApi } from './sessions.ts'
 import type { HostApi } from './host.ts'
+import type { FileTreeApi } from './filetree.ts'
 import type { WorkspaceApi } from './workspace.ts'
 import type { AgentPresetsApi } from './agent-presets.ts'
 import type { SkillsApi } from './skills.ts'
@@ -23,6 +24,7 @@ export interface ApiProxy {
   sessions: SessionsApi
   subagents: SubagentsApi
   host: HostApi
+  filetree: FileTreeApi
   workspace: WorkspaceApi
   skills: SkillsApi
   agentPresets: AgentPresetsApi
@@ -48,6 +50,9 @@ export type {
   SessionListMetadata, SessionProjectionsBlock, SessionSearchItem, SessionsApi, SessionSummary,
 } from './sessions.ts'
 export type { DirectoryEntry, DirectoryListing, HostApi } from './host.ts'
+export type {
+  FileTreeApi, FileTreeEntry, FileTreeEntryKind, FileTreeGitStatus, FileTreeListing, FileTreeSearchResult,
+} from './filetree.ts'
 export type {
   SubagentAddress, SubagentCatalog, SubagentInterruptReceipt, SubagentListEntry,
   SubagentPromptReceipt, SubagentsApi,
diff --git a/packages/host/apiproxy/src/api/rpc-map.ts b/packages/host/apiproxy/src/api/rpc-map.ts
index 80dede1799..e445c26554 100644
--- a/packages/host/apiproxy/src/api/rpc-map.ts
+++ b/packages/host/apiproxy/src/api/rpc-map.ts
@@ -6,6 +6,7 @@
 
 import type { SessionsApi } from './sessions.ts'
 import type { HostApi } from './host.ts'
+import type { FileTreeApi } from './filetree.ts'
 import type { WorkspaceApi } from './workspace.ts'
 import type { AgentPresetsApi } from './agent-presets.ts'
 import type { SkillsApi } from './skills.ts'
@@ -43,6 +44,9 @@ export interface RpcMethodMap {
   'host.listDirectory': HostApi['listDirectory']
   'host.createDirectory': HostApi['createDirectory']
   'host.openPath': HostApi['openPath']
+  'filetree.list': FileTreeApi['list']
+  'filetree.search': FileTreeApi['search']
+  'filetree.select': FileTreeApi['select']
   'workspace.list': WorkspaceApi['list']
   'workspace.create': WorkspaceApi['create']
   'workspace.rename': WorkspaceApi['rename']
diff --git a/packages/host/apiproxy/src/api/rpc.schema.ts b/packages/host/apiproxy/src/api/rpc.schema.ts
index 177f0ffd29..361c587fd4 100644
--- a/packages/host/apiproxy/src/api/rpc.schema.ts
+++ b/packages/host/apiproxy/src/api/rpc.schema.ts
@@ -47,6 +47,8 @@ export const rpcErrorSchema: z.ZodType<RpcError> = z.discriminatedUnion('code',
   z.object({ code: z.literal('directory-exists'), message: z.string(), details: z.object({ path: z.string() }) }),
   z.object({ code: z.literal('directory-create-failed'), message: z.string(), details: z.object({ path: z.string() }) }),
   z.object({ code: z.literal('directory-picker-unavailable'), message: z.string(), details: z.object({ capability: z.string() }) }),
+  z.object({ code: z.literal('file-tree-unavailable'), message: z.string(), details: z.object({}) }),
+  z.object({ code: z.literal('tree-unreadable'), message: z.string(), details: z.object({ path: z.string() }) }),
   z.object({ code: z.literal('agent-preset-read-only'), message: z.string(), details: z.object({ agentPreset: z.string(), reason: z.string() }) }),
   z.object({ code: z.literal('agent-preset-locked'), message: z.string(), details: z.object({ sessionId: z.string(), agentPreset: z.string() }) }),
   z.object({ code: z.literal('agent-preset-conflict'), message: z.string(), details: z.object({ sessionId: z.string(), requestedPreset: z.string(), existingPreset: z.string().optional() }) }),
diff --git a/packages/host/apiproxy/src/api/rpc.ts b/packages/host/apiproxy/src/api/rpc.ts
index 134a39eba9..3fc7f00c4c 100644
--- a/packages/host/apiproxy/src/api/rpc.ts
+++ b/packages/host/apiproxy/src/api/rpc.ts
@@ -45,6 +45,8 @@ export interface RpcErrorDetailsMap {
   'directory-exists': { path: string }
   'directory-create-failed': { path: string }
   'directory-picker-unavailable': { capability: string }
+  'file-tree-unavailable': {}
+  'tree-unreadable': { path: string }
   'agent-preset-read-only': { agentPreset: string; reason: string }
   'agent-preset-locked': { sessionId: SessionId; agentPreset: string }
   'agent-preset-conflict': { sessionId: SessionId; requestedPreset: string; existingPreset?: string }
diff --git a/packages/host/apiproxy/src/fetch/client.ts b/packages/host/apiproxy/src/fetch/client.ts
index 70e3ece58f..cb101a3b59 100644
--- a/packages/host/apiproxy/src/fetch/client.ts
+++ b/packages/host/apiproxy/src/fetch/client.ts
@@ -17,6 +17,9 @@ import {
   hostCreateDirectoryValueSchema, hostDescribeValueSchema,
   hostListDirectoryValueSchema, hostOpenPathValueSchema, hostPickDirectoryValueSchema,
 } from '../api/host.schema.ts'
+import {
+  fileTreeListValueSchema, fileTreeSearchValueSchema, fileTreeSelectValueSchema,
+} from '../api/filetree.schema.ts'
 import {
   sessionCancelValueSchema,
   sessionAttachmentValueSchema,
@@ -112,6 +115,11 @@ export interface IApiClient {
     createDirectory(payload: RequestPayload<'host.createDirectory'>, signal?: AbortSignal): Promise<RpcResponse<ResponseValue<'host.createDirectory'>>>
     openPath(payload: RequestPayload<'host.openPath'>, signal?: AbortSignal): Promise<RpcResponse<ResponseValue<'host.openPath'>>>
   }
+  filetree: {
+    list(payload: RequestPayload<'filetree.list'>, signal?: AbortSignal): Promise<RpcResponse<ResponseValue<'filetree.list'>>>
+    search(payload: RequestPayload<'filetree.search'>, signal?: AbortSignal): Promise<RpcResponse<ResponseValue<'filetree.search'>>>
+    select(payload: RequestPayload<'filetree.select'>, signal?: AbortSignal): Promise<RpcResponse<ResponseValue<'filetree.select'>>>
+  }
   workspace: {
     list(payload: RequestPayload<'workspace.list'>, signal?: AbortSignal): Promise<RpcResponse<ResponseValue<'workspace.list'>>>
     create(payload: RequestPayload<'workspace.create'>, signal?: AbortSignal): Promise<RpcResponse<ResponseValue<'workspace.create'>>>
@@ -191,6 +199,9 @@ const UNARY_VALUE_SCHEMAS: { [K in keyof RpcMethodMap]: z.ZodType<Wire<ResponseV
   'host.listDirectory': hostListDirectoryValueSchema,
   'host.createDirectory': hostCreateDirectoryValueSchema,
   'host.openPath': hostOpenPathValueSchema,
+  'filetree.list': fileTreeListValueSchema,
+  'filetree.search': fileTreeSearchValueSchema,
+  'filetree.select': fileTreeSelectValueSchema,
   'workspace.list': workspaceListValueSchema,
   'workspace.create': workspaceCreateValueSchema,
   'workspace.rename': workspaceRenameValueSchema,
@@ -443,6 +454,12 @@ export abstract class AbstractApiClient implements IApiClient {
     openPath: (payload, signal) => this.callUnary('host.openPath', payload, signal),
   }
 
+  readonly filetree: IApiClient['filetree'] = {
+    list: (payload, signal) => this.callUnary('filetree.list', payload, signal),
+    search: (payload, signal) => this.callUnary('filetree.search', payload, signal),
+    select: (payload, signal) => this.callUnary('filetree.select', payload, signal),
+  }
+
   readonly workspace: IApiClient['workspace'] = {
     list: (payload, signal) => this.callUnary('workspace.list', payload, signal),
     create: (payload, signal) => this.callUnary('workspace.create', payload, signal),
diff --git a/packages/host/apiproxy/src/fetch/handler.ts b/packages/host/apiproxy/src/fetch/handler.ts
index 697171ec53..4204ab9fa1 100644
--- a/packages/host/apiproxy/src/fetch/handler.ts
+++ b/packages/host/apiproxy/src/fetch/handler.ts
@@ -34,6 +34,9 @@ import {
   hostListDirectoryRequestSchema, hostOpenPathRequestSchema,
   hostPickDirectoryRequestSchema,
 } from '../api/host.schema.ts'
+import {
+  fileTreeListRequestSchema, fileTreeSearchRequestSchema, fileTreeSelectRequestSchema,
+} from '../api/filetree.schema.ts'
 import {
   workspaceArchiveSessionRequestSchema,
   workspaceCreateRequestSchema,
@@ -109,6 +112,9 @@ const UNARY_ROUTES: UnaryRoutes = {
   'host.listDirectory': { schema: hostListDirectoryRequestSchema, invoke: (api, r, signal) => api.host.listDirectory(r, signal) },
   'host.createDirectory': { schema: hostCreateDirectoryRequestSchema, invoke: (api, r) => api.host.createDirectory(r) },
   'host.openPath': { schema: hostOpenPathRequestSchema, invoke: (api, r, signal) => api.host.openPath(r, signal) },
+  'filetree.list': { schema: fileTreeListRequestSchema, invoke: (api, r, signal) => api.filetree.list(r, signal) },
+  'filetree.search': { schema: fileTreeSearchRequestSchema, invoke: (api, r, signal) => api.filetree.search(r, signal) },
+  'filetree.select': { schema: fileTreeSelectRequestSchema, invoke: (api, r) => api.filetree.select(r) },
   'workspace.list': { schema: workspaceListRequestSchema, invoke: (api, r) => api.workspace.list(r) },
   'workspace.create': { schema: workspaceCreateRequestSchema, invoke: (api, r) => api.workspace.create(r) },
   'workspace.rename': { schema: workspaceRenameRequestSchema, invoke: (api, r) => api.workspace.rename(r) },
diff --git a/packages/host/apiproxy/src/index.ts b/packages/host/apiproxy/src/index.ts
index ac6c770801..7734164aec 100644
--- a/packages/host/apiproxy/src/index.ts
+++ b/packages/host/apiproxy/src/index.ts
@@ -83,6 +83,7 @@ export class ApiProxyService extends Service implements ApiProxy {
   readonly subagents: ApiProxy['subagents']
   readonly workspace: ApiProxy['workspace']
   readonly host: ApiProxy['host']
+  readonly filetree: ApiProxy['filetree']
   readonly goals: ApiProxy['goals']
   readonly skills: ApiProxy['skills']
   readonly agentPresets: ApiProxy['agentPresets']
@@ -111,6 +112,7 @@ export class ApiProxyService extends Service implements ApiProxy {
     this.subagents = api.subagents
     this.workspace = api.workspace
     this.host = api.host
+    this.filetree = api.filetree
     this.goals = api.goals
     this.skills = api.skills
     this.agentPresets = api.agentPresets
diff --git a/packages/host/apiproxy/tests/api-proxy-workspace.spec.ts b/packages/host/apiproxy/tests/api-proxy-workspace.spec.ts
index 54bdb015e4..3051d97e92 100644
--- a/packages/host/apiproxy/tests/api-proxy-workspace.spec.ts
+++ b/packages/host/apiproxy/tests/api-proxy-workspace.spec.ts
@@ -12,6 +12,8 @@ import { DomainFacility } from '@deepseek-ai/dsh-storage-domain'
 import UserQuestionService from '@deepseek-ai/dsh-user-questions'
 import { DirectoryPickerError } from '@deepseek-ai/dsh-host-directory-picker'
 import type { DirectoryPickerCapability } from '@deepseek-ai/dsh-host-directory-picker'
+import { FileTreeError } from '@deepseek-ai/dsh-host-file-tree'
+import type { FileTreeListing, FileTreeSearchResult } from '@deepseek-ai/dsh-host-file-tree'
 import WorkspaceRegistry from '@deepseek-ai/dsh-workspace'
 import type { HostFrame, WorkspaceId } from '@deepseek-ai/dsh-host-apiproxy/api'
 import type { RpcRequest, RpcResponse } from '@deepseek-ai/dsh-host-apiproxy/api/rpc'
@@ -64,6 +66,11 @@ async function harness(
   extras: {
     openPath?: (path: string, signal: AbortSignal) => Promise<void>
     canOpenPath?: () => boolean
+    fileTree?: {
+      listDir: (path: string, signal?: AbortSignal) => Promise<FileTreeListing>
+      search?: (root: string, query: string, signal?: AbortSignal) => Promise<FileTreeSearchResult>
+    }
+    fileSelection?: { select: (session: Session, files: readonly string[]) => void }
   } = {},
 ) {
   const ctx = new Context()
@@ -102,6 +109,8 @@ async function harness(
   // Structural picker fake: the gateway only reads capability(); a stable
   // object per harness mirrors the seam's stability contract.
   ctx.provide('directoryPicker', { capability: () => picker } as never)
+  if (extras.fileTree !== undefined) ctx.provide('fileTree', extras.fileTree as never)
+  if (extras.fileSelection !== undefined) ctx.provide('fileSelection', extras.fileSelection as never)
   const api = createApiProxy(ctx, {
     defaultModelSelection: () => ({ provider: 'test', model: 'test-model' }),
     cwd: root,
@@ -228,6 +237,102 @@ describe('host.listDirectory / host.createDirectory', () => {
   })
 })
 
+describe('filetree.list', () => {
+  it('lists one level through the composed file-tree backend', async () => {
+    const { api } = await harness(undefined, { kind: 'native', pick: async () => null }, {
+      fileTree: {
+        listDir: async path => ({
+          path,
+          entries: [{ name: 'a.txt', path: `${path}/a.txt`, kind: 'file', hidden: false, gitStatus: 'modified' }],
+          truncated: false,
+        }),
+      },
+    })
+    const result = expectOk(await api.filetree.list(request({ path: '/proj' }), new AbortController().signal))
+    expect(result.path).toBe('/proj')
+    expect(result.entries[0]).toMatchObject({ name: 'a.txt', kind: 'file', gitStatus: 'modified' })
+    expect(result.truncated).toBe(false)
+  })
+
+  it('fails with file-tree-unavailable when no backend is composed', async () => {
+    const { api } = await harness()
+    expect((await api.filetree.list(request({ path: '/proj' }), new AbortController().signal)).result)
+      .toMatchObject({ ok: false, error: { code: 'file-tree-unavailable' } })
+  })
+
+  it('maps a FileTreeError to tree-unreadable', async () => {
+    const { api } = await harness(undefined, { kind: 'native', pick: async () => null }, {
+      fileTree: {
+        listDir: async () => { throw new FileTreeError('tree-unreadable', '/denied', 'cannot list /denied') },
+      },
+    })
+    expect((await api.filetree.list(request({ path: '/denied' }), new AbortController().signal)).result)
+      .toMatchObject({ ok: false, error: { code: 'tree-unreadable', details: { path: '/denied' } } })
+  })
+
+  it('fails select with file-tree-unavailable when no selection service is composed', async () => {
+    const { api } = await harness()
+    expect((await api.filetree.select(request({ sessionId: 's1' as never, files: ['a.txt'] }))).result)
+      .toMatchObject({ ok: false, error: { code: 'file-tree-unavailable' } })
+  })
+})
+
+describe('filetree.search', () => {
+  it('searches names through the composed file-tree backend', async () => {
+    const received: string[] = []
+    const { api } = await harness(undefined, { kind: 'native', pick: async () => null }, {
+      fileTree: {
+        listDir: async path => ({ path, entries: [], truncated: false }),
+        search: async (root, query) => {
+          received.push(`${root}:${query}`)
+          return {
+            path: root,
+            matches: [{ name: 'a.txt', path: `${root}/dir/a.txt`, kind: 'file', hidden: false, gitStatus: 'untracked' }],
+            truncated: false,
+          }
+        },
+      },
+    })
+    const result = expectOk(await api.filetree.search(request({ path: '/proj', query: 'a' }), new AbortController().signal))
+    expect(received).toEqual(['/proj:a'])
+    expect(result.path).toBe('/proj')
+    expect(result.matches[0]).toMatchObject({ name: 'a.txt', kind: 'file', gitStatus: 'untracked' })
+    expect(result.truncated).toBe(false)
+  })
+
+  it('fails with file-tree-unavailable when no backend is composed', async () => {
+    const { api } = await harness()
+    expect((await api.filetree.search(request({ path: '/proj', query: 'a' }), new AbortController().signal)).result)
+      .toMatchObject({ ok: false, error: { code: 'file-tree-unavailable' } })
+  })
+
+  it('maps a FileTreeError to tree-unreadable', async () => {
+    const { api } = await harness(undefined, { kind: 'native', pick: async () => null }, {
+      fileTree: {
+        listDir: async () => ({ path: '/denied', entries: [], truncated: false }),
+        search: async () => { throw new FileTreeError('tree-unreadable', '/denied', 'cannot search /denied') },
+      },
+    })
+    expect((await api.filetree.search(request({ path: '/denied', query: 'a' }), new AbortController().signal)).result)
+      .toMatchObject({ ok: false, error: { code: 'tree-unreadable', details: { path: '/denied' } } })
+  })
+
+  it('propagates abort into the backend as a cancelled RPC error', async () => {
+    const { api } = await harness(undefined, { kind: 'native', pick: async () => null }, {
+      fileTree: {
+        listDir: async () => ({ path: '/proj', entries: [], truncated: false }),
+        search: (_root, _query, signal) => new Promise((_resolve, reject) => {
+          signal?.addEventListener('abort', () => { reject(new Error('search aborted')) }, { once: true })
+        }),
+      },
+    })
+    const abort = new AbortController()
+    const pending = api.filetree.search(request({ path: '/proj', query: 'a' }), abort.signal)
+    abort.abort()
+    expect((await pending).result).toMatchObject({ ok: false, error: { code: 'cancelled' } })
+  })
+})
+
 describe('host.openPath', () => {
   it('describes whether this deployment can reach a user-visible native desktop', async () => {
     const visible = await harness(undefined, undefined, { canOpenPath: () => true })
diff --git a/packages/host/apiproxy/tests/client-handler.spec.ts b/packages/host/apiproxy/tests/client-handler.spec.ts
index 4130d8f210..916298d7f8 100644
--- a/packages/host/apiproxy/tests/client-handler.spec.ts
+++ b/packages/host/apiproxy/tests/client-handler.spec.ts
@@ -21,6 +21,7 @@ function scriptedApi(overrides: {
   sessions?: Partial<ApiProxy['sessions']>
   subagents?: Partial<ApiProxy['subagents']>
   host?: Partial<ApiProxy['host']>
+  filetree?: Partial<ApiProxy['filetree']>
   skills?: Partial<ApiProxy['skills']>
   agentPresets?: Partial<ApiProxy['agentPresets']>
   events?: Partial<ApiProxy['events']>
@@ -80,6 +81,12 @@ function scriptedApi(overrides: {
       openPath: r => ok(r, { opened: true as const }),
       ...overrides.host,
     },
+    filetree: {
+      list: r => ok(r, { path: '/t', entries: [], truncated: false }),
+      search: r => ok(r, { path: r.payload.path, matches: [], truncated: false }),
+      select: r => ok(r, { selected: [...r.payload.files] }),
+      ...overrides.filetree,
+    },
     workspace: {
       list: r => ok(r, { items: [], archivedSessionIds: [] }),
       create: r => ok(r, { workspace: { workspaceId: 'w1' as never, path: '/t', title: 't', sessionIds: [], createdAt: '0', updatedAt: '0' }, created: true }),
diff --git a/packages/host/apiproxy/tests/fetch-carrier.spec.ts b/packages/host/apiproxy/tests/fetch-carrier.spec.ts
index 2000f708ba..0c543ebad3 100644
--- a/packages/host/apiproxy/tests/fetch-carrier.spec.ts
+++ b/packages/host/apiproxy/tests/fetch-carrier.spec.ts
@@ -160,6 +160,17 @@ function fakeApi(overrides: Partial<{ muxFrames: MuxFrame[]; hostFrames: HostFra
         return { rpcId: request.rpcId, result: { ok: true, value: { opened: true as const } } }
       },
     },
+    filetree: {
+      async list(request) {
+        return { rpcId: request.rpcId, result: { ok: true, value: { path: '/w', entries: [], truncated: false } } }
+      },
+      async search(request) {
+        return { rpcId: request.rpcId, result: { ok: true, value: { path: request.payload.path, matches: [], truncated: false } } }
+      },
+      async select(request) {
+        return { rpcId: request.rpcId, result: { ok: true, value: { selected: [...request.payload.files] } } }
+      },
+    },
     workspace: {
       async list(request) {
         return { rpcId: request.rpcId, result: { ok: true, value: { items: [], archivedSessionIds: [] } } }
diff --git a/packages/host/apiproxy/tsconfig.json b/packages/host/apiproxy/tsconfig.json
index 3ed38b3d77..997b84ff59 100644
--- a/packages/host/apiproxy/tsconfig.json
+++ b/packages/host/apiproxy/tsconfig.json
@@ -92,6 +92,12 @@
     {
       "path": "../directory-picker"
     },
+    {
+      "path": "../file-tree"
+    },
+    {
+      "path": "../../context/file-selection"
+    },
     {
       "path": "../../extensions/cordis-host-runner"
     },
diff --git a/packages/test-support/client-runtime/src/workspaces.ts b/packages/test-support/client-runtime/src/workspaces.ts
index 4f6b2122cb..ea9f327401 100644
--- a/packages/test-support/client-runtime/src/workspaces.ts
+++ b/packages/test-support/client-runtime/src/workspaces.ts
@@ -1,7 +1,8 @@
 /** Test-owned workspaces face: the renderer standard-kit observable plus recorded actions. */
 import { createSnapshotStore } from '@deepseek-ai/dsh-client-runtime/client'
 import type {
-  DirectoryListing, IWorkspaces, SessionId, SnapshotStore, WorkspaceId, WorkspaceListState, WorkspaceView,
+  DirectoryListing, FileTreeListing, FileTreeSearchResult, IWorkspaces, SessionId, SnapshotStore, WorkspaceId,
+  WorkspaceListState, WorkspaceView,
 } from '@deepseek-ai/dsh-client-runtime/client'
 import { workspaceListState } from './fixtures.ts'
 import type { Stabilizer } from './fixtures.ts'
@@ -137,6 +138,39 @@ export class TestWorkspaces implements IWorkspaces {
     }
   }
 
+  /**
+   * File-tree listing (recorded). The default serves an empty level; stub to
+   * shape a tree.
+   * @param path - absolute directory to list (a session's workspace root).
+   * @returns the level's listing.
+   */
+  async listDir(path: string, signal?: AbortSignal): Promise<FileTreeListing> {
+    this.calls.push({ method: 'listDir', args: [path, signal] })
+    const stub = this.stubs.get('listDir')
+    if (stub !== undefined) return await (stub(path, signal) as Promise<FileTreeListing>)
+    return { path, entries: [], truncated: false }
+  }
+
+  /**
+   * File-tree name search (recorded). The default serves an empty result;
+   * stub to shape matches.
+   * @param root - absolute search root (a session's workspace root).
+   * @param query - non-blank name substring.
+   * @returns the flat match set.
+   */
+  async searchEntries(root: string, query: string, signal?: AbortSignal): Promise<FileTreeSearchResult> {
+    this.calls.push({ method: 'searchEntries', args: [root, query, signal] })
+    const stub = this.stubs.get('searchEntries')
+    if (stub !== undefined) return await (stub(root, query, signal) as Promise<FileTreeSearchResult>)
+    return { path: root, matches: [], truncated: false }
+  }
+
+  async selectFiles(sessionId: SessionId, files: string[]): Promise<void> {
+    this.calls.push({ method: 'selectFiles', args: [sessionId, files] })
+    const stub = this.stubs.get('selectFiles')
+    if (stub !== undefined) await (stub(sessionId, files) as Promise<void>)
+  }
+
   /**
    * Browse child creation (recorded). The default joins parent and name.
    * @param path - absolute existing parent directory.
diff --git a/tsconfig.base.json b/tsconfig.base.json
index 16d69db6db..ec5377282e 100644
--- a/tsconfig.base.json
+++ b/tsconfig.base.json
@@ -165,6 +165,10 @@
       "@deepseek-ai/dsh-host-directory-picker-native/*": ["./packages/host/directory-picker-native/src/*"],
       "@deepseek-ai/dsh-host-directory-picker-auto": ["./packages/host/directory-picker-auto/src"],
       "@deepseek-ai/dsh-host-directory-picker-auto/*": ["./packages/host/directory-picker-auto/src/*"],
+      "@deepseek-ai/dsh-host-file-tree": ["./packages/host/file-tree/src"],
+      "@deepseek-ai/dsh-host-file-tree/*": ["./packages/host/file-tree/src/*"],
+      "@deepseek-ai/dsh-host-file-tree-local": ["./packages/host/file-tree-local/src"],
+      "@deepseek-ai/dsh-host-file-tree-local/*": ["./packages/host/file-tree-local/src/*"],
       "@deepseek-ai/dsh-host-apiproxy/client": ["./packages/host/apiproxy/src/fetch/client.ts"],
       "@deepseek-ai/dsh-host-apiproxy/*": ["./packages/host/apiproxy/src/*"],
       "@deepseek-ai/dsh-host-webserver": ["./packages/host/webserver/src"],
diff --git a/tsconfig.client.json b/tsconfig.client.json
index e599f76f1b..a2bd065d23 100644
--- a/tsconfig.client.json
+++ b/tsconfig.client.json
@@ -76,6 +76,7 @@
     { "path": "./packages/client/ui-jobs" },
     { "path": "./packages/client/ui-directory-picker-browse" },
     { "path": "./packages/client/ui-directory-picker-native" },
+    { "path": "./packages/client/ui-file-tree" },
     { "path": "./packages/client/ui-goal" },
     { "path": "./packages/client/ui-message-feedback" },
     { "path": "./packages/client/ui-model-selection" },
diff --git a/tsconfig.host.json b/tsconfig.host.json
index 459036247a..41a140ef05 100644
--- a/tsconfig.host.json
+++ b/tsconfig.host.json
@@ -172,6 +172,7 @@
     { "path": "./packages/feedback/command-feedback" },
     { "path": "./packages/schedule/schedule" },
     { "path": "./packages/context/time-context" },
+    { "path": "./packages/context/file-selection" },
     { "path": "./packages/context/tmux-context" },
     { "path": "./packages/context/session-reference" },
     { "path": "./packages/interaction/user-questions" },
@@ -285,6 +286,8 @@
     { "path": "./packages/host/directory-picker-auto" },
     { "path": "./packages/host/directory-picker-browse" },
     { "path": "./packages/host/directory-picker-native" },
+    { "path": "./packages/host/file-tree" },
+    { "path": "./packages/host/file-tree-local" },
     { "path": "./packages/host/frontend-static" },
     { "path": "./packages/host/plugin-inventory" },
     { "path": "./packages/host/webserver" },
diff --git a/docs/persistence-catalog.md b/docs/persistence-catalog.md
index 608a8c9..4970726 100644
--- a/docs/persistence-catalog.md
+++ b/docs/persistence-catalog.md
@@ -422,6 +422,22 @@ Source: [`packages/feedback/command-feedback/src/index.ts:62`](../packages/feedb
 
 Source: [`packages/context/file-selection/src/types.ts:13`](../packages/context/file-selection/src/types.ts)
 
+<a id="fileannotation--log-only"></a>
+
+#### `file/annotation` — log-only
+
+```ts persistence-catalog
+/**
+ * The user changed the file tree's edit-marker set (annotated text regions
+ * and their instructions). Replaces the prior set (an empty `annotations`
+ * clears it). Logged so the model-visible markers and the client's tags are
+ * reconstructable from the session log.
+ */
+'file/annotation': { annotations: FileAnnotation[] }
+```
+
+Source: [`packages/context/file-selection/src/types.ts:46`](../packages/context/file-selection/src/types.ts)
+
 ### `goal/*`
 
 <a id="goalchange--log-only"></a>
diff --git a/packages/api/remotes/src/client/index.ts b/packages/api/remotes/src/client/index.ts
index 7bd7fe2..7662c10 100644
--- a/packages/api/remotes/src/client/index.ts
+++ b/packages/api/remotes/src/client/index.ts
@@ -35,8 +35,8 @@ export type {} from '@deepseek-ai/dsh-host-file-tree/types'
  */
 export type {
   ClientResponse, ConfigurableProviderView, ConnectionHandle, ConnectionSinks, ContentBlock,
-  CredentialView, DirectoryListing, DiscoveredModelView, FileTreeEntry, FileTreeEntryKind, FileTreeGitStatus,
-  FileTreeListing, FileTreeSearchResult, HistoryEntry, HostFrame, IApiClient,
+  CredentialView, DirectoryListing, DiscoveredModelView, FileAnnotation, FileAnnotationStatus, FileTreeEntry, FileTreeEntryKind, FileTreeGitStatus,
+  FileTreeListing, FileTreeReadResult, FileTreeSearchResult, HistoryEntry, HostFrame, IApiClient,
   MessageId, ModelCatalogFailure, ModelProviderGroup, ModelReasoningEffort, ModelSelection,
   MuxFrame, PromptContentPart, QuestionResponsePayload, QueueAction, RpcError, RpcId, RpcReceipt,
   RpcRequest, RpcResponse, RpcResult, SessionId, SessionModels, SessionSearchItem,
diff --git a/packages/client/connection/src/client/api.ts b/packages/client/connection/src/client/api.ts
index 7414cf2..0285e5a 100644
--- a/packages/client/connection/src/client/api.ts
+++ b/packages/client/connection/src/client/api.ts
@@ -8,7 +8,7 @@
 export type {
   ApiProxy, SessionsApi, SessionSearchItem, SessionSummary, PromptContentPart, HostApi, EventsApi, MuxFrame, HostFrame,
   ApprovalResponsePayload, QuestionResponsePayload, HistoryEntry, ToolEventView,
-  DirectoryEntry, DirectoryListing, FileTreeEntry, FileTreeEntryKind, FileTreeGitStatus, FileTreeListing, FileTreeSearchResult,
+  DirectoryEntry, DirectoryListing, FileAnnotation, FileAnnotationStatus, FileTreeEntry, FileTreeEntryKind, FileTreeGitStatus, FileTreeListing, FileTreeReadResult, FileTreeSearchResult,
   ResponseValue, WorkspaceApi, WorkspaceId, WorkspaceView,
   SkillsApi, SkillEntry,
   ModelCatalogFailure, ModelCatalogModel, ModelProviderGroup, ModelReasoning,
diff --git a/packages/client/connection/src/client/fixture.ts b/packages/client/connection/src/client/fixture.ts
index 7c8425e..5b421fc 100644
--- a/packages/client/connection/src/client/fixture.ts
+++ b/packages/client/connection/src/client/fixture.ts
@@ -2567,6 +2567,9 @@ function createFixtureWorld(options: FixtureOptions): FixtureWorld {
       list: request => ok(request, { path: request.payload.path, entries: [], truncated: false }),
       search: request => ok(request, { path: request.payload.path, matches: [], truncated: false }),
       select: request => ok(request, { selected: [...request.payload.files] }),
+      read: request => ok(request, { path: request.payload.path, text: '', truncated: false }),
+      annotate: request => ok(request, { annotations: [...request.payload.annotations] }),
+      annotations: request => ok(request, { annotations: [] }),
     },
     workspace: {
       list: request => ok(request, {
@@ -3106,6 +3109,9 @@ export class FixtureApiClient extends AbstractApiClient {
       case 'filetree.list': return this.api.filetree.list(request, signal)
       case 'filetree.search': return this.api.filetree.search(request, signal)
       case 'filetree.select': return this.api.filetree.select(request)
+      case 'filetree.read': return this.api.filetree.read(request, signal)
+      case 'filetree.annotate': return this.api.filetree.annotate(request)
+      case 'filetree.annotations': return this.api.filetree.annotations(request)
       case 'workspace.list': return this.api.workspace.list(request)
       case 'workspace.create': return this.api.workspace.create(request)
       case 'workspace.rename': return this.api.workspace.rename(request)
diff --git a/packages/client/connection/src/client/index.ts b/packages/client/connection/src/client/index.ts
index d311669..c63d4a8 100644
--- a/packages/client/connection/src/client/index.ts
+++ b/packages/client/connection/src/client/index.ts
@@ -16,7 +16,7 @@ import type { ClientConnectionRpc } from '../rpc.ts'
 export type {
   ApiProxy, SessionsApi, SessionSearchItem, SessionSummary, PromptContentPart, HostApi, EventsApi, MuxFrame, HostFrame,
   ApprovalResponsePayload, QuestionResponsePayload, HistoryEntry, ToolEventView,
-  DirectoryEntry, DirectoryListing, FileTreeEntry, FileTreeEntryKind, FileTreeGitStatus, FileTreeListing, FileTreeSearchResult,
+  DirectoryEntry, DirectoryListing, FileAnnotation, FileAnnotationStatus, FileTreeEntry, FileTreeEntryKind, FileTreeGitStatus, FileTreeListing, FileTreeReadResult, FileTreeSearchResult,
   ToolCallView, ToolResultView, WorkspaceApi, WorkspaceId, WorkspaceView,
   SkillsApi, SkillEntry,
   ModelCatalogFailure, ModelCatalogModel, ModelProviderGroup, ModelReasoning,
diff --git a/packages/client/connection/tests/fake-api.client.ts b/packages/client/connection/tests/fake-api.client.ts
index 205d696..1818ac6 100644
--- a/packages/client/connection/tests/fake-api.client.ts
+++ b/packages/client/connection/tests/fake-api.client.ts
@@ -152,6 +152,9 @@ export class FakeApiClient implements IApiClient {
     list: payload => this.record('filetree.list', payload, Promise.resolve(ok({ path: '/f', entries: [], truncated: false }))),
     search: payload => this.record('filetree.search', payload, Promise.resolve(ok({ path: '/f', matches: [], truncated: false }))),
     select: payload => this.record('filetree.select', payload, Promise.resolve(ok({ selected: [...(payload as { files: string[] }).files] }))),
+    read: payload => this.record('filetree.read', payload, Promise.resolve(ok({ path: '/f', text: '', truncated: false }))),
+    annotate: payload => this.record('filetree.annotate', payload, Promise.resolve(ok({ annotations: [] }))),
+    annotations: payload => this.record('filetree.annotations', payload, Promise.resolve(ok({ annotations: [] }))),
   }
 
   readonly workspace: IApiClient['workspace'] = {
diff --git a/packages/client/runtime/src/client/contract/workspaces.ts b/packages/client/runtime/src/client/contract/workspaces.ts
index f85644f..2525033 100644
--- a/packages/client/runtime/src/client/contract/workspaces.ts
+++ b/packages/client/runtime/src/client/contract/workspaces.ts
@@ -6,7 +6,7 @@
  * the concrete class. Widening this interface is the explicit act of
  * widening what features may do to the workspaces domain.
  */
-import type { DirectoryListing, FileTreeListing, FileTreeSearchResult, SessionId, WorkspaceId, WorkspaceView } from '@deepseek-ai/dsh-api-remotes/client'
+import type { DirectoryListing, FileAnnotation, FileTreeListing, FileTreeReadResult, FileTreeSearchResult, SessionId, WorkspaceId, WorkspaceView } from '@deepseek-ai/dsh-api-remotes/client'
 import type { WorkspaceListState } from '../workspaces/service.ts'
 import type { ObservableSnapshot } from './store.ts'
 
@@ -79,6 +79,25 @@ export interface IWorkspaces {
    * @param files - the complete selected-path set.
    */
   selectFiles(sessionId: SessionId, files: string[]): Promise<void>
+  /**
+   * Read one text file for the file-tree editor surface.
+   * @param path - absolute file to read.
+   * @param signal - aborts the wire request and the Host's read.
+   * @returns the decoded text prefix and a language hint.
+   */
+  readFile(path: string, signal?: AbortSignal): Promise<FileTreeReadResult>
+  /**
+   * Record a session's complete edit-marker set.
+   * @param sessionId - the session whose markers changed.
+   * @param annotations - the complete marker list.
+   */
+  annotateFiles(sessionId: SessionId, annotations: FileAnnotation[]): Promise<void>
+  /**
+   * Read a session's latest edit-marker set (with completion statuses).
+   * @param sessionId - the session to read.
+   * @returns the markers in recorded order.
+   */
+  readAnnotations(sessionId: SessionId): Promise<FileAnnotation[]>
   /**
    * Rename a Workspace.
    * @param workspaceId - target workspace.
diff --git a/packages/client/runtime/src/client/index.ts b/packages/client/runtime/src/client/index.ts
index 8329f3a..09847f0 100644
--- a/packages/client/runtime/src/client/index.ts
+++ b/packages/client/runtime/src/client/index.ts
@@ -64,8 +64,8 @@ export type { SubagentAddress, JobView } from '@deepseek-ai/dsh-client-connectio
 export type { WorkspaceListPhase } from './workspaces/manager.ts'
 export type { WorkspaceListState } from './workspaces/service.ts'
 export type {
-  DirectoryEntry, DirectoryListing, FileTreeEntry, FileTreeEntryKind, FileTreeGitStatus, FileTreeListing,
-  FileTreeSearchResult, WorkspaceId, WorkspaceView,
+  DirectoryEntry, DirectoryListing, FileAnnotation, FileAnnotationStatus, FileTreeEntry, FileTreeEntryKind, FileTreeGitStatus, FileTreeListing,
+  FileTreeReadResult, FileTreeSearchResult, WorkspaceId, WorkspaceView,
 } from '@deepseek-ai/dsh-client-connection/client'
 // Runtime owns the snapshot store; web-react only binds it to React.
 export { createSnapshotStore, defineStore, shallowEqual } from './contract/store.ts'
diff --git a/packages/client/runtime/src/client/workspaces/service.ts b/packages/client/runtime/src/client/workspaces/service.ts
index a1bba43..75cdadd 100644
--- a/packages/client/runtime/src/client/workspaces/service.ts
+++ b/packages/client/runtime/src/client/workspaces/service.ts
@@ -2,7 +2,7 @@
 
 import type { Context } from '@deepseek-ai/cordis'
 import type {
-  DirectoryListing, FileTreeListing, FileTreeSearchResult, IApiClient, RpcError,
+  DirectoryListing, FileAnnotation, FileTreeListing, FileTreeReadResult, FileTreeSearchResult, IApiClient, RpcError,
   SessionId, WorkspaceId, WorkspaceView,
 } from '@deepseek-ai/dsh-api-remotes/client'
 import type { SnapshotStore } from '../contract/store.ts'
@@ -284,6 +284,39 @@ export class WorkspaceRuntime implements IWorkspaces {
     if (!response.result.ok) throw new Error(`file tree selection failed: ${response.result.error.message}`)
   }
 
+  /**
+   * Read one text file for the file-tree editor surface.
+   * @param path - absolute file to read.
+   * @param signal - aborts the wire request and the Host's read.
+   * @returns the decoded text prefix and a language hint.
+   */
+  async readFile(path: string, signal?: AbortSignal): Promise<FileTreeReadResult> {
+    const response = await this.api.filetree.read({ path }, signal)
+    if (!response.result.ok) throw new Error(`file tree read failed: ${response.result.error.message}`)
+    return response.result.value
+  }
+
+  /**
+   * Record a session's complete edit-marker set.
+   * @param sessionId - the session whose markers changed.
+   * @param annotations - the complete marker list.
+   */
+  async annotateFiles(sessionId: SessionId, annotations: FileAnnotation[]): Promise<void> {
+    const response = await this.api.filetree.annotate({ sessionId, annotations })
+    if (!response.result.ok) throw new Error(`file tree annotate failed: ${response.result.error.message}`)
+  }
+
+  /**
+   * Read a session's latest edit-marker set (with completion statuses).
+   * @param sessionId - the session to read.
+   * @returns the markers in recorded order.
+   */
+  async readAnnotations(sessionId: SessionId): Promise<FileAnnotation[]> {
+    const response = await this.api.filetree.annotations({ sessionId })
+    if (!response.result.ok) throw new Error(`file tree annotations failed: ${response.result.error.message}`)
+    return response.result.value.annotations
+  }
+
   /**
    * Rename a Workspace.
    * @param workspaceId - target workspace.
diff --git a/packages/client/runtime/tests/fake-api.client.ts b/packages/client/runtime/tests/fake-api.client.ts
index 9936616..af9826f 100644
--- a/packages/client/runtime/tests/fake-api.client.ts
+++ b/packages/client/runtime/tests/fake-api.client.ts
@@ -191,6 +191,9 @@ export class FakeApiClient implements IApiClient {
     list: (payload: unknown) => this.record('filetree.list', payload, this.onListDir(payload)),
     search: (payload: unknown) => this.record('filetree.search', payload, Promise.resolve(ok({ path: '/f', matches: [], truncated: false }))),
     select: (payload: unknown) => this.record('filetree.select', payload, this.onSelect(payload)),
+    read: (payload: unknown) => this.record('filetree.read', payload, Promise.resolve(ok({ path: '/f', text: '', truncated: false }))),
+    annotate: (payload: unknown) => this.record('filetree.annotate', payload, Promise.resolve(ok({ annotations: [] }))),
+    annotations: (payload: unknown) => this.record('filetree.annotations', payload, Promise.resolve(ok({ annotations: [] }))),
   }
 
   // The archive-set field defaults at the binding below so list stubs keep
diff --git a/packages/core/session/src/known-event-types.ts b/packages/core/session/src/known-event-types.ts
index 9cfe71c..147282f 100644
--- a/packages/core/session/src/known-event-types.ts
+++ b/packages/core/session/src/known-event-types.ts
@@ -32,6 +32,7 @@ export const KNOWN_SESSION_EVENT_TYPES: ReadonlySet<string> = new Set([
   'compaction/summary',
   'feedback/record',
   'file/selection',
+  'file/annotation',
   'goal/change',
   'hook/invoked',
   'hook/result',
diff --git a/packages/host/apiproxy/src/api-proxy.ts b/packages/host/apiproxy/src/api-proxy.ts
index 3a29389..f843ae6 100644
--- a/packages/host/apiproxy/src/api-proxy.ts
+++ b/packages/host/apiproxy/src/api-proxy.ts
@@ -637,7 +637,7 @@ function directoryError(error: unknown): RpcError {
 /** Map a file-tree listing failure onto the wire error vocabulary (unknown throws stay internal). */
 function fileTreeError(error: unknown): RpcError {
   if (error instanceof FileTreeError) {
-    return { code: 'tree-unreadable', message: error.message, details: { path: error.path } }
+    return { code: error.code, message: error.message, details: { path: error.path } }
   }
   return { code: 'internal', message: error instanceof Error ? error.message : String(error), details: {} }
 }
@@ -3075,6 +3075,54 @@ export function createApiProxy(ctx: Context, defaults: ApiProxyDefaults): ApiPro
         fileSelection.select(found.agent.session, request.payload.files)
         return ok(request, { selected: [...request.payload.files] })
       },
+
+      async read(request, signal) {
+        const fileTree = ctx.get('fileTree')
+        if (fileTree === undefined) {
+          return err(request, {
+            code: 'file-tree-unavailable',
+            message: 'filetree.read needs a file-tree backend; none is composed',
+            details: {},
+          })
+        }
+        try {
+          return ok(request, await fileTree.readFile(request.payload.path, signal))
+        } catch (error: unknown) {
+          if (signal.aborted) {
+            return err(request, { code: 'cancelled', message: 'file tree read was aborted', details: {} })
+          }
+          return err(request, fileTreeError(error))
+        }
+      },
+
+      async annotate(request) {
+        const fileSelection = ctx.get('fileSelection')
+        if (fileSelection === undefined) {
+          return err(request, {
+            code: 'file-tree-unavailable',
+            message: 'filetree.annotate needs a file-selection service; none is composed',
+            details: {},
+          })
+        }
+        const found = await agentFor(request.payload.sessionId)
+        if ('error' in found) return err(request, found.error)
+        fileSelection.annotate(found.agent.session, request.payload.annotations)
+        return ok(request, { annotations: request.payload.annotations.map(a => ({ ...a })) })
+      },
+
+      async annotations(request) {
+        const fileSelection = ctx.get('fileSelection')
+        if (fileSelection === undefined) {
+          return err(request, {
+            code: 'file-tree-unavailable',
+            message: 'filetree.annotations needs a file-selection service; none is composed',
+            details: {},
+          })
+        }
+        const found = await agentFor(request.payload.sessionId)
+        if ('error' in found) return err(request, found.error)
+        return ok(request, { annotations: fileSelection.latestAnnotations(found.agent.session) })
+      },
     },
 
     goals: {
diff --git a/packages/host/apiproxy/src/api/index.ts b/packages/host/apiproxy/src/api/index.ts
index a5cb5b7..9986401 100644
--- a/packages/host/apiproxy/src/api/index.ts
+++ b/packages/host/apiproxy/src/api/index.ts
@@ -51,7 +51,8 @@ export type {
 } from './sessions.ts'
 export type { DirectoryEntry, DirectoryListing, HostApi } from './host.ts'
 export type {
-  FileTreeApi, FileTreeEntry, FileTreeEntryKind, FileTreeGitStatus, FileTreeListing, FileTreeSearchResult,
+  FileAnnotation, FileAnnotationStatus, FileTreeApi, FileTreeEntry, FileTreeEntryKind, FileTreeGitStatus,
+  FileTreeListing, FileTreeReadResult, FileTreeSearchResult,
 } from './filetree.ts'
 export type {
   SubagentAddress, SubagentCatalog, SubagentInterruptReceipt, SubagentListEntry,
diff --git a/packages/host/apiproxy/src/api/rpc-map.ts b/packages/host/apiproxy/src/api/rpc-map.ts
index e445c26..5293d1f 100644
--- a/packages/host/apiproxy/src/api/rpc-map.ts
+++ b/packages/host/apiproxy/src/api/rpc-map.ts
@@ -47,6 +47,9 @@ export interface RpcMethodMap {
   'filetree.list': FileTreeApi['list']
   'filetree.search': FileTreeApi['search']
   'filetree.select': FileTreeApi['select']
+  'filetree.read': FileTreeApi['read']
+  'filetree.annotate': FileTreeApi['annotate']
+  'filetree.annotations': FileTreeApi['annotations']
   'workspace.list': WorkspaceApi['list']
   'workspace.create': WorkspaceApi['create']
   'workspace.rename': WorkspaceApi['rename']
diff --git a/packages/host/apiproxy/src/api/rpc.schema.ts b/packages/host/apiproxy/src/api/rpc.schema.ts
index 361c587..9c15625 100644
--- a/packages/host/apiproxy/src/api/rpc.schema.ts
+++ b/packages/host/apiproxy/src/api/rpc.schema.ts
@@ -49,6 +49,7 @@ export const rpcErrorSchema: z.ZodType<RpcError> = z.discriminatedUnion('code',
   z.object({ code: z.literal('directory-picker-unavailable'), message: z.string(), details: z.object({ capability: z.string() }) }),
   z.object({ code: z.literal('file-tree-unavailable'), message: z.string(), details: z.object({}) }),
   z.object({ code: z.literal('tree-unreadable'), message: z.string(), details: z.object({ path: z.string() }) }),
+  z.object({ code: z.literal('not-a-text-file'), message: z.string(), details: z.object({ path: z.string() }) }),
   z.object({ code: z.literal('agent-preset-read-only'), message: z.string(), details: z.object({ agentPreset: z.string(), reason: z.string() }) }),
   z.object({ code: z.literal('agent-preset-locked'), message: z.string(), details: z.object({ sessionId: z.string(), agentPreset: z.string() }) }),
   z.object({ code: z.literal('agent-preset-conflict'), message: z.string(), details: z.object({ sessionId: z.string(), requestedPreset: z.string(), existingPreset: z.string().optional() }) }),
diff --git a/packages/host/apiproxy/src/api/rpc.ts b/packages/host/apiproxy/src/api/rpc.ts
index 3fc7f00..c1b4daa 100644
--- a/packages/host/apiproxy/src/api/rpc.ts
+++ b/packages/host/apiproxy/src/api/rpc.ts
@@ -47,6 +47,7 @@ export interface RpcErrorDetailsMap {
   'directory-picker-unavailable': { capability: string }
   'file-tree-unavailable': {}
   'tree-unreadable': { path: string }
+  'not-a-text-file': { path: string }
   'agent-preset-read-only': { agentPreset: string; reason: string }
   'agent-preset-locked': { sessionId: SessionId; agentPreset: string }
   'agent-preset-conflict': { sessionId: SessionId; requestedPreset: string; existingPreset?: string }
diff --git a/packages/host/apiproxy/src/fetch/client.ts b/packages/host/apiproxy/src/fetch/client.ts
index cb101a3..860b770 100644
--- a/packages/host/apiproxy/src/fetch/client.ts
+++ b/packages/host/apiproxy/src/fetch/client.ts
@@ -18,7 +18,8 @@ import {
   hostListDirectoryValueSchema, hostOpenPathValueSchema, hostPickDirectoryValueSchema,
 } from '../api/host.schema.ts'
 import {
-  fileTreeListValueSchema, fileTreeSearchValueSchema, fileTreeSelectValueSchema,
+  fileTreeAnnotationsValueSchema, fileTreeAnnotateValueSchema, fileTreeListValueSchema,
+  fileTreeReadValueSchema, fileTreeSearchValueSchema, fileTreeSelectValueSchema,
 } from '../api/filetree.schema.ts'
 import {
   sessionCancelValueSchema,
@@ -119,6 +120,9 @@ export interface IApiClient {
     list(payload: RequestPayload<'filetree.list'>, signal?: AbortSignal): Promise<RpcResponse<ResponseValue<'filetree.list'>>>
     search(payload: RequestPayload<'filetree.search'>, signal?: AbortSignal): Promise<RpcResponse<ResponseValue<'filetree.search'>>>
     select(payload: RequestPayload<'filetree.select'>, signal?: AbortSignal): Promise<RpcResponse<ResponseValue<'filetree.select'>>>
+    read(payload: RequestPayload<'filetree.read'>, signal?: AbortSignal): Promise<RpcResponse<ResponseValue<'filetree.read'>>>
+    annotate(payload: RequestPayload<'filetree.annotate'>, signal?: AbortSignal): Promise<RpcResponse<ResponseValue<'filetree.annotate'>>>
+    annotations(payload: RequestPayload<'filetree.annotations'>, signal?: AbortSignal): Promise<RpcResponse<ResponseValue<'filetree.annotations'>>>
   }
   workspace: {
     list(payload: RequestPayload<'workspace.list'>, signal?: AbortSignal): Promise<RpcResponse<ResponseValue<'workspace.list'>>>
@@ -202,6 +206,9 @@ const UNARY_VALUE_SCHEMAS: { [K in keyof RpcMethodMap]: z.ZodType<Wire<ResponseV
   'filetree.list': fileTreeListValueSchema,
   'filetree.search': fileTreeSearchValueSchema,
   'filetree.select': fileTreeSelectValueSchema,
+  'filetree.read': fileTreeReadValueSchema,
+  'filetree.annotate': fileTreeAnnotateValueSchema,
+  'filetree.annotations': fileTreeAnnotationsValueSchema,
   'workspace.list': workspaceListValueSchema,
   'workspace.create': workspaceCreateValueSchema,
   'workspace.rename': workspaceRenameValueSchema,
@@ -458,6 +465,9 @@ export abstract class AbstractApiClient implements IApiClient {
     list: (payload, signal) => this.callUnary('filetree.list', payload, signal),
     search: (payload, signal) => this.callUnary('filetree.search', payload, signal),
     select: (payload, signal) => this.callUnary('filetree.select', payload, signal),
+    read: (payload, signal) => this.callUnary('filetree.read', payload, signal),
+    annotate: (payload, signal) => this.callUnary('filetree.annotate', payload, signal),
+    annotations: (payload, signal) => this.callUnary('filetree.annotations', payload, signal),
   }
 
   readonly workspace: IApiClient['workspace'] = {
diff --git a/packages/host/apiproxy/src/fetch/handler.ts b/packages/host/apiproxy/src/fetch/handler.ts
index 4204ab9..b98c34c 100644
--- a/packages/host/apiproxy/src/fetch/handler.ts
+++ b/packages/host/apiproxy/src/fetch/handler.ts
@@ -35,7 +35,8 @@ import {
   hostPickDirectoryRequestSchema,
 } from '../api/host.schema.ts'
 import {
-  fileTreeListRequestSchema, fileTreeSearchRequestSchema, fileTreeSelectRequestSchema,
+  fileTreeAnnotationsRequestSchema, fileTreeAnnotateRequestSchema, fileTreeListRequestSchema,
+  fileTreeReadRequestSchema, fileTreeSearchRequestSchema, fileTreeSelectRequestSchema,
 } from '../api/filetree.schema.ts'
 import {
   workspaceArchiveSessionRequestSchema,
@@ -115,6 +116,9 @@ const UNARY_ROUTES: UnaryRoutes = {
   'filetree.list': { schema: fileTreeListRequestSchema, invoke: (api, r, signal) => api.filetree.list(r, signal) },
   'filetree.search': { schema: fileTreeSearchRequestSchema, invoke: (api, r, signal) => api.filetree.search(r, signal) },
   'filetree.select': { schema: fileTreeSelectRequestSchema, invoke: (api, r) => api.filetree.select(r) },
+  'filetree.read': { schema: fileTreeReadRequestSchema, invoke: (api, r, signal) => api.filetree.read(r, signal) },
+  'filetree.annotate': { schema: fileTreeAnnotateRequestSchema, invoke: (api, r) => api.filetree.annotate(r) },
+  'filetree.annotations': { schema: fileTreeAnnotationsRequestSchema, invoke: (api, r) => api.filetree.annotations(r) },
   'workspace.list': { schema: workspaceListRequestSchema, invoke: (api, r) => api.workspace.list(r) },
   'workspace.create': { schema: workspaceCreateRequestSchema, invoke: (api, r) => api.workspace.create(r) },
   'workspace.rename': { schema: workspaceRenameRequestSchema, invoke: (api, r) => api.workspace.rename(r) },
diff --git a/packages/test-support/client-runtime/src/workspaces.ts b/packages/test-support/client-runtime/src/workspaces.ts
index ea9f327..5ebbaec 100644
--- a/packages/test-support/client-runtime/src/workspaces.ts
+++ b/packages/test-support/client-runtime/src/workspaces.ts
@@ -1,7 +1,7 @@
 /** Test-owned workspaces face: the renderer standard-kit observable plus recorded actions. */
 import { createSnapshotStore } from '@deepseek-ai/dsh-client-runtime/client'
 import type {
-  DirectoryListing, FileTreeListing, FileTreeSearchResult, IWorkspaces, SessionId, SnapshotStore, WorkspaceId,
+  DirectoryListing, FileAnnotation, FileTreeListing, FileTreeReadResult, FileTreeSearchResult, IWorkspaces, SessionId, SnapshotStore, WorkspaceId,
   WorkspaceListState, WorkspaceView,
 } from '@deepseek-ai/dsh-client-runtime/client'
 import { workspaceListState } from './fixtures.ts'
@@ -171,6 +171,26 @@ export class TestWorkspaces implements IWorkspaces {
     if (stub !== undefined) await (stub(sessionId, files) as Promise<void>)
   }
 
+  async readFile(path: string, signal?: AbortSignal): Promise<FileTreeReadResult> {
+    this.calls.push({ method: 'readFile', args: [path, signal] })
+    const stub = this.stubs.get('readFile')
+    if (stub !== undefined) return await (stub(path, signal) as Promise<FileTreeReadResult>)
+    return { path, text: '', truncated: false }
+  }
+
+  async annotateFiles(sessionId: SessionId, annotations: FileAnnotation[]): Promise<void> {
+    this.calls.push({ method: 'annotateFiles', args: [sessionId, annotations] })
+    const stub = this.stubs.get('annotateFiles')
+    if (stub !== undefined) await (stub(sessionId, annotations) as Promise<void>)
+  }
+
+  async readAnnotations(sessionId: SessionId): Promise<FileAnnotation[]> {
+    this.calls.push({ method: 'readAnnotations', args: [sessionId] })
+    const stub = this.stubs.get('readAnnotations')
+    if (stub !== undefined) return await (stub(sessionId) as Promise<FileAnnotation[]>)
+    return []
+  }
+
   /**
    * Browse child creation (recorded). The default joins parent and name.
    * @param path - absolute existing parent directory.

diff --git a/packages/client/ui-primitives/src/index.ts b/packages/client/ui-primitives/src/index.ts
--- a/packages/client/ui-primitives/src/index.ts
+++ b/packages/client/ui-primitives/src/index.ts
@@ -41,6 +41,8 @@ export { WebBlock } from './WebBlock.tsx'
 export type { WebBlockProps, WebSearchBlockProps, WebFetchBlockProps, WebSourceView } from './WebBlock.tsx'
 export { CodeBlock } from './markdown/CodeBlock.tsx'
 export type { CodeBlockProps } from './markdown/CodeBlock.tsx'
+export { grammarLoadCount, highlightLines, subscribeGrammarLoaded } from './markdown/highlight.ts'
+export type { HighlightSpan } from './markdown/highlight.ts'
 export { JsonBlock } from './markdown/JsonBlock.tsx'
 export { MarkdownText } from './markdown/MarkdownText.tsx'
 export type { MarkdownCodeLabels, MarkdownFileMentions } from './markdown/MarkdownText.tsx'